{
  "service": {
    "name": "<%= @prom_tag %>",
    "tags": ["<%= @prom_tag %>"],
    "port": 9100,
    "checks": [
      {
        "script": "/usr/bin/curl -sf localhost:9100/metrics >/dev/null",
        "interval": "10s"
      }
    ]
  }
}
